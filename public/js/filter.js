const FILTER_SELECTOR = "js-filter";
const CATEGORY_PARENT_SELECTOR = "js-cat-container";

const getCategories = () => {
  const categoryParentElems = document.querySelectorAll(
    `.${CATEGORY_PARENT_SELECTOR}`
  );
};
